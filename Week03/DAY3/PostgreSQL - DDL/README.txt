:point_right: LAB LEARNINGS:
- If some column have space in the name, I need to put the name into "" ;
- If the type of column is varchar, I need to feed the data using '', if can't use "" ;
- I need to put ; at the end of each command, because the space and the enter is equivalent;
- Smallint para guardar números até 35.000, maior que isso, usar 'integer';
- Os nomes da coluna são com todos com letra minúscula por default, se quise-los com a inicial maiscula, escreve-lo entre "";
